# Hugo build output (generated; do not commit)
public/

# Hugo generated resources cache (generated; do not commit)
resources/_gen/

# Hugo build lock and optional stats
.hugo_build.lock
# If a future pipeline needs this as an input, remove from ignore and commit it
hugo_stats.json

# Node.js dependencies and logs (only relevant if you adopt Node tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# When/if you adopt a package manager, commit ONE lockfile for reproducible builds and keep the other ignored:
# package-lock.json
# yarn.lock

# Operating System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor and IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary/cache files
*.tmp
*.temp
.cache/

# Log files
*.log

# Environment files
.env
.env.local
.env.*.local
